[package]
name = "reactor_samples"
version = "0.0.0"
edition = "2024"
publish = false

[dependencies]
windows-reactor = { workspace = true, features = ["winui-backend"] }
windows-canvas = { workspace = true, features = ["reactor"] }

[build-dependencies]
windows-reactor-setup = { workspace = true }

[lints]
workspace = true
