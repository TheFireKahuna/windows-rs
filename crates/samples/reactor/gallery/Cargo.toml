[package]
name = "reactor_gallery"
version = "0.0.0"
edition = "2024"
publish = false

[dependencies]
windows-reactor = { workspace = true, features = ["winui-backend"] }

[build-dependencies]
windows-reactor-setup = { workspace = true }

[lints]
workspace = true
