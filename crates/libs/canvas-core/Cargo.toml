[package]
name = "windows-canvas-core"
version = "0.0.0"
edition = "2024"
rust-version = "1.95"
license = "MIT OR Apache-2.0"
description = "Windows 2D graphics library — reactor-free drawing core"
repository = "https://github.com/microsoft/windows-rs"
categories = ["os::windows-apis"]

[dependencies]
windows-core = { workspace = true }
windows-numerics = { workspace = true }

[lints]
workspace = true
